{
  "name": "cultwatch-mobile",
  "version": "1.2.0",
  "description": "CultWatch for Android — the same situation room, wrapped in Capacitor",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "sync": "node ../scripts/build-mobile.js && cap sync android",
    "open": "cap open android",
    "apk": "node ../scripts/build-mobile.js && cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/browser": "^6.0.4",
    "@capacitor/core": "^6.2.0",
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
